<UserControl x:Class="WsusManager.Views.MaintenanceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="1000"
             Background="{StaticResource BackgroundBrush}">

    <Grid Margin="24">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="16"/>
            <ColumnDefinition Width="400"/>
        </Grid.ColumnDefinitions>

        <!-- Left Panel -->
        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <StackPanel Grid.Row="0" Margin="0,0,0,24">
                <TextBlock Text="Maintenance" Style="{StaticResource HeaderTextStyle}"/>
                <TextBlock Text="Deep cleanup and content management operations"
                           Style="{StaticResource CaptionTextStyle}"/>
            </StackPanel>

            <!-- Deep Cleanup Options -->
            <Border Grid.Row="1" Style="{StaticResource CardStyle}">
                <StackPanel>
                    <TextBlock Text="Deep Cleanup" Style="{StaticResource SubheaderTextStyle}"/>
                    <TextBlock Text="Select cleanup operations to perform:"
                               Style="{StaticResource CaptionTextStyle}"
                               Margin="0,0,0,16"/>

                    <StackPanel Margin="0,0,0,16">
                        <CheckBox Content="Remove declined supersession records"
                                  IsChecked="{Binding RemoveDeclined}"
                                  Margin="0,0,0,8"/>
                        <CheckBox Content="Remove superseded supersession records"
                                  IsChecked="{Binding RemoveSuperseded}"
                                  Margin="0,0,0,8"/>
                        <CheckBox Content="Optimize database indexes"
                                  IsChecked="{Binding OptimizeIndexes}"
                                  Margin="0,0,0,8"/>
                        <CheckBox Content="Update database statistics"
                                  IsChecked="{Binding UpdateStatistics}"
                                  Margin="0,0,0,8"/>
                        <CheckBox Content="Shrink database (may take a long time)"
                                  IsChecked="{Binding ShrinkDatabase}"/>
                    </StackPanel>

                    <Button Content="Run Deep Cleanup"
                            Command="{Binding RunDeepCleanupCommand}"
                            Style="{StaticResource PrimaryButtonStyle}"
                            Height="40"/>

                    <!-- Progress -->
                    <StackPanel Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}"
                                Margin="0,16,0,0">
                        <TextBlock Text="{Binding CurrentOperation}"
                                   Style="{StaticResource BodyTextStyle}"
                                   Margin="0,0,0,8"/>
                        <ProgressBar Value="{Binding OperationProgress}"
                                     Maximum="100"
                                     Height="8"
                                     Style="{StaticResource ModernProgressBarStyle}"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- Content Reset -->
            <Border Grid.Row="2" Style="{StaticResource CardStyle}" Margin="0,16,0,0">
                <StackPanel>
                    <TextBlock Text="Content Reset" Style="{StaticResource SubheaderTextStyle}"/>
                    <TextBlock Style="{StaticResource BodyTextStyle}" TextWrapping="Wrap" Margin="0,0,0,16">
                        Reset the WSUS content download process. This will verify all content files
                        and re-download any that are missing or corrupted.
                    </TextBlock>

                    <Border Background="#FFF4E5"
                            BorderBrush="{StaticResource WarningBrush}"
                            BorderThickness="1"
                            CornerRadius="4"
                            Padding="12"
                            Margin="0,0,0,16">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Warning:" FontWeight="Bold" Foreground="{StaticResource WarningBrush}" Margin="0,0,8,0"/>
                            <TextBlock Text="This operation may take 30-90 minutes to complete."
                                       Style="{StaticResource BodyTextStyle}"/>
                        </StackPanel>
                    </Border>

                    <Button Content="Reset Content Download"
                            Command="{Binding RunContentResetCommand}"
                            Style="{StaticResource DangerButtonStyle}"
                            Height="40"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Right Panel - Output Log -->
        <Border Grid.Column="2" Style="{StaticResource CardStyle}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="0,0,0,8">
                    <TextBlock Text="Operation Log" Style="{StaticResource SubheaderTextStyle}"/>
                    <Button Content="Clear"
                            Command="{Binding ClearLogCommand}"
                            Style="{StaticResource SecondaryButtonStyle}"
                            HorizontalAlignment="Right"
                            Padding="8,4"/>
                </Grid>

                <ListBox Grid.Row="1"
                         ItemsSource="{Binding OutputLog}"
                         Style="{StaticResource ModernListBoxStyle}"
                         FontFamily="Consolas"
                         FontSize="11"/>

                <TextBlock Grid.Row="2"
                           Text="{Binding StatusMessage}"
                           Style="{StaticResource CaptionTextStyle}"
                           Margin="0,8,0,0"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>
